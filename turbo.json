{
	"$schema": "https://turbo.build/schema.json",
	"globalDependencies": ["fixtures/complex-turbo-monorepo/**", "scripts/fixtures/**"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": []
		},
		"test": {
			"dependsOn": ["^build"]
		},
		"test:integration": {
			"dependsOn": ["^build"]
		},
		"typecheck": {
			"dependsOn": ["^typecheck"]
		}
	}
}
